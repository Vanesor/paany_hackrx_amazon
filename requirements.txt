# Core ML dependencies (actually used in main.py)
torch>=1.13.0,<2.1.0
transformers>=4.21.0,<4.36.0
sentence-transformers>=2.2.2
numpy>=1.21.0,<1.25.0

# Document processing (used for PDF extraction)
PyMuPDF>=1.20.0
requests>=2.28.0

# FastAPI and server (main API framework)
fastapi>=0.95.0,<0.105.0
uvicorn>=0.20.0,<0.24.0
pydantic>=1.10.0,<2.5.0

# Google Generative AI (Gemini model)
google-generativeai==0.3.2

# Essential utilities (actually imported)
tqdm>=4.64.0
psutil>=5.9.0
python-dotenv>=0.19.0

# Text processing (used for chunking and tokenization)
nltk>=3.7
tiktoken>=0.5.0

# Async HTTP (used in document downloading)
aiohttp>=3.8.0,<3.9.0

# Scientific computing (used for similarity calculations)
scikit-learn>=1.1.0,<1.4.0
